name='vis'
version='95bf9f59f8a9a37148bdc0787db378d62c7cd032'
sources="https://github.com/martanne/vis/archive/${version}.tar.gz"
depends='libtermkey make lpeg lua ncurses'
build() {
./configure \
    --prefix=/usr \
    --enable-lua \
    --enable-lpeg-static

make
make DESTDIR="$INSTALL" install
}
